{
  "name": "wally-astro-package",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "demo",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "preview": "npm run preview --workspaces",
    "dev:demo": "npm run dev --workspace=demo",
    "dev:pkg": "npm run dev --workspace=astro-google-reviews"
  }
}